<div class="region inset theme-{{props.theme or 'background'}} {{props.classes}}">
    <div class="region inset prose stack align-center text-center gap-xs">
        <p class="overline">{{props.content.overline}}</p>
        <h1>{{props.content.headline}}</h1>
        <p>
            {{props.content.body}}
        </p>
    </div>
</div>