import { css } from 'lit'

export const borderRadiusStyles = css`
  :root {
    --radius-s: 0.25rem;
    --radius-m: 0.5rem;
    --radius-l: 1rem;
    --radius-pill: 999px;
    --radius-circle: 50%;
  }
`
