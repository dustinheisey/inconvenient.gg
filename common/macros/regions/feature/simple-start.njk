<div class="region prose stack align-start text-start gap-xs theme-{{props.theme or 'background'}} {{props.classes}}">
    <p class="overline">{{props.content.overline}}</p>
    <h1>{{props.content.headline}}</h1>
    <p>
        {{props.content.body}}
    </p>
</div>