<div class="region sidebar reverse sidebar-split gap-m theme-{{props.theme or 'background'}} {{props.classes}}">
    <div class="frame square">
        {% set src = './public/img/' + props.img.src%}
        {% img src, props.img.alt%}
    </div>
    <div class="prose fixed">
        <p class="overline">{{props.content.overline}}</p>
        <h2>{{props.content.headline}}</h2>
        <p>{{props.content.body}}</p>
        <a href="{{props.content.btn.href}}" class="btn {% if props.content.btn.variant %}btn-{{props.content.btn.variant}}{% endif %}">{{props.content.btn.label}}</a>
    </div>
</div>