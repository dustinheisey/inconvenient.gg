import { Canvas, Meta, Controls } from '@storybook/blocks'

import * as Stories from './error.story.js'

<Meta of={Stories} />

# Error Page

the error page is shown when a link is not found in the website.

<Canvas of={Stories.Default} sourceState="shown" withToolbar="true" />
