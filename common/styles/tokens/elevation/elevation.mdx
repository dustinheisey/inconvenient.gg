import { Meta } from '@storybook/blocks'

<Meta title="Tokens/Elevation" parameters={{ status: { type: 'stable' } }} />

## Elevation

<table>
  <thead>
    <tr>
      <th>Token</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`--shadow-xs`</td>
      <td>
        <div
          className="elevation-demo"
          style={{
            backgroundColor: 'var(--color-surface-light)',
            width: 'var(--space-xl)',
            height: 'var(--space-xl)',
            borderRadius: 'var(--radius-s)',
            boxShadow: 'var(--shadow-xs)'
          }}
        ></div>
      </td>
    </tr>
    <tr>
      <td>`--shadow-s`</td>
      <td>
        <div
          className="elevation-demo"
          style={{
            backgroundColor: 'var(--color-surface-light)',
            width: 'var(--space-xl)',
            height: 'var(--space-xl)',
            borderRadius: 'var(--radius-s)',
            boxShadow: 'var(--shadow-s)'
          }}
        ></div>
      </td>
    </tr>
    <tr>
      <td>`--shadow-m`</td>
      <td>
        <div
          className="elevation-demo"
          style={{
            backgroundColor: 'var(--color-surface-light)',
            width: 'var(--space-xl)',
            height: 'var(--space-xl)',
            borderRadius: 'var(--radius-s)',
            boxShadow: 'var(--shadow-m)'
          }}
        ></div>
      </td>
    </tr>
    <tr>
      <td>`--shadow-l`</td>
      <td>
        <div
          className="elevation-demo"
          style={{
            backgroundColor: 'var(--color-surface-light)',
            width: 'var(--space-xl)',
            height: 'var(--space-xl)',
            borderRadius: 'var(--radius-s)',
            boxShadow: 'var(--shadow-l)'
          }}
        ></div>
      </td>
    </tr>
    <tr>
      <td>`--shadow-xl`</td>
      <td>
        <div
          className="elevation-demo"
          style={{
            backgroundColor: 'var(--color-surface-light)',
            width: 'var(--space-xl)',
            height: 'var(--space-xl)',
            borderRadius: 'var(--radius-s)',
            boxShadow: 'var(--shadow-xl)'
          }}
        ></div>
      </td>
    </tr>
  </tbody>
</table>
